const cards=$$('[data-task-card]');$$('[data-task-filter]').forEach(btn=>btn.addEventListener('click',()=>{$$('[data-task-filter]').forEach(x=>x.classList.remove('active'));btn.classList.add('active');const filter=btn.dataset.taskFilter;cards.forEach(card=>card.classList.toggle('hidden',filter!=='all'&&card.dataset.status!==filter))}));document.addEventListener('click',async e=>{const check=e.target.closest('[data-task-id]');if(check){try{await json('/api/tasks/'+check.dataset.taskId+'/complete',{method:'POST'});toast('Task completed');setTimeout(()=>location.reload(),450)}catch(err){toast(err.message,'error')}}const del=e.target.closest('[data-delete-task]');if(del&&confirm('Delete this task?')){try{await json('/api/tasks/'+del.dataset.deleteTask,{method:'DELETE'});document.querySelector('[data-task-card="'+del.dataset.deleteTask+'"]')?.remove();toast('Task deleted')}catch(err){toast(err.message,'error')}}});
