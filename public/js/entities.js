$$('.quick-entity-form').forEach(form=>form.addEventListener('submit',async e=>{e.preventDefault();const entity=form.dataset.entity;const data=Object.fromEntries(new FormData(form));try{await json('/api/'+(entity==='goal'?'goals':entity+'s'),{method:'POST',body:JSON.stringify(data)});toast(entity[0].toUpperCase()+entity.slice(1)+' created');setTimeout(()=>location.reload(),450)}catch(err){toast(err.message,'error')}}));
